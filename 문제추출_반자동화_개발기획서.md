# Summit AI — PDF 기출문제 반자동 추출·분류 기능 개발기획서

> 작성일: 2026-05-31 (개정: 분류체계 재정립 반영)
> 대상: 종이/디지털 PDF 시험지 → AI 1차 추출 → 사람 검토 → **"기출문제(써밋용)"** 신규 은행에 확정 저장
> 설계 원칙: **퀄리티 최우선. 자동화는 보조, 최종 책임은 사람 검토.**

---

## 0. 요약 (TL;DR)

PDF를 올리면 AI가 문항을 1차 추출·분류해 **검토 대기열(staging)**에 쌓고,
운영자가 전용 웹 UI(`/ingest`)에서 모든 필드를 수정·확정한 뒤,
**사이드바에 새로 만드는 "기출문제(써밋용)" 은행**에 저장한다. (기존 24,000문항 은행과 완전 분리)

```
PDF 업로드(+학년 일괄지정) → 텍스트/OCR 추출 → AI 문항분해
   → 3축 분류(정답선지 기반 문법유형 · 학년 · 형식) + 신뢰도 플래깅
   → 검토 대기열 → 웹 검토/수정 UI → 확정 → summit_json 파일 append → git push
```

### ★ 써밋용 3축 분류체계 (이번 개정의 핵심) ★
1. **세부 문법 유형** — **"정답 선지가 나타내는 문법"** 으로 분류. (해설·오답 선지의 문법으로 분류하지 않음)
2. **학년** — `중1 / 중2 / 중3`. **파일 업로드 시 파일 전체에 일괄 지정**(문항별 추론 안 함).
3. **문제 형식** — `문장형 / 지문형 / 서술형`.

### 제거 필드
`학교명(school_name)`, `난이도(상/중/하)`, `publisher`, `region`, `lesson_tag`, `year`, `source_file`, `sentences`, `main_category`, `tags` 등 **부가 메타는 전부 제외**. 난이도 필터 UI도 제거.

---

## 0.5 실행 체크리스트 (★"계속" 진행용 — 이 문서 하나로 재개★)

> **사용법**: 사용자가 **"계속"** 이라고만 하면, 아래 "진행 상태"의 *다음 작업*부터 순서대로 1~2개 단위로 구현하고, 완료 시 체크박스와 진행 상태를 갱신한다. `[BLOCK]` 표시는 사용자 입력(예: API 키)이 있어야 진행 가능 — 도달하면 멈추고 무엇이 필요한지 알린다.
> 작업은 **API 키 없이 가능한 것(Phase 0·1)을 먼저** 배치했다. 키가 준비되면 Phase 2를 이어간다.

### 진행 상태 (resume pointer) — *작업 시 항상 여기부터 갱신*
- **다음 작업**: (Phase 2 전체 완료) → 실사용 피드백 후 `T3.x`(디지털텍스트 경로/self-consistency/도표 crop/일괄 되돌리기/중복감지) 중 선택
- **완료**: `T0.1`~`T0.5`, `T1.1`~`T1.3`, `T2.0`~`T2.6` (**Phase 0·1·2 전체 — 2026-06-01**). PDF 업로드→AI추출→검토 UI→확정 저장 전 구간 동작·검증 완료.
- **결정됨(2026-06-01)**: ① 혼합형 → 미분류(99-1)+플래그, 검토 수동 분류. ② 모델 현행 유지. ③ **비문법은 버리지 않고 `out_of_scope` draft로 보존** → 검토화면 "제외됨" 접이식 목록 노출 + 잘못 걸러진 어법은 **"어법으로 승격"**(`/api/ingest/promote`, Stage B 재실행) 가능. 비문법은 승격 전엔 확정 불가(은행 오염 방지).
- **모델**: MODEL_OCR=gpt-5.4-nano, MODEL_SOLVE=gpt-5.5 (`.env`)
- **참고**: SPA 라우트는 서버 직접접근 시 404(기존 `/testbank` 등 공통, catch_all fallback 미동작) — 사이드바 클릭(클라이언트 라우팅)으로는 정상. 필요 시 별도 수정 가능(현재 범위 밖).
- **사용 모델(기본값, .env로 교체 가능)**: OCR/분해/선별 = nano(최신 GPT nano), 정답풀이/분류 = 상위 모델(mini 이상). 코드는 모델명을 `.env`에서 읽어 모델 비종속.

### Phase 0 — 토대 (API 키 불필요) ✅ 완료
- [x] **T0.1** `summit_json/` 생성 + 빈 파일 `중1/중2/중3/고1/고2/고3.json` (각 `{"questions": []}`)
- [x] **T0.2** 백엔드 모듈 `summit_ingest.py`: 써밋 스키마 **validator** + `draft→summit 매퍼` + 소항목 ID 집합 검증(`^\d+-\d+$` & 유효목록)
- [x] **T0.3** 카테고리 단일소스 공유: `grammarCategories.js` 소항목 91개를 `summit_categories.json`로 export
- [x] **T0.4** `app.py`에 `load_summit_db()` + `GET /api/summit/questions`(categories/type 필터) + `GET /api/summit/categories`
- [x] **T0.5** 기존 24k 로더/엔드포인트 무변경 회귀확인(`/api/questions` 19,656건 정상)

### Phase 1 — 신규 "기출문제(써밋용)" 은행 화면 (API 키 불필요) ✅ 완료
- [x] **T1.1** `Sidebar.jsx` 메뉴 "기출문제 (써밋용)" + `App.jsx` 라우트 `/summit-bank`
- [x] **T1.2** `pages/SummitBank.jsx`: 3축 필터(학년 / 문법트리 `CategoryTree` 재사용 / 형식 토글), 난이도 없음, `QuestionCard` 재사용, 인쇄
- [x] **T1.3** 샘플 문항 3개를 `summit_json/고3.json`에 투입 → 빌드 통과 + 필터(학년/문법/형식) 동작 확인

### Phase 2 — 스캔/OCR 추출 + 검토 UI (API 키 필요)
- [x] **T2.0** `.env`에 `OPENAI_API_KEY` + `MODEL_OCR=gpt-5.4-nano`/`MODEL_SOLVE=gpt-5.5`/`INGEST_MODE=1`. `.gitignore` 확인됨.
- [x] **T2.1** `requirements.txt`에 `PyMuPDF`,`pdfplumber` 추가 + `summit_pdf.py`(텍스트레이어 자동판별 / 이미지화 / 2단 컬럼 분리)
- [x] **T2.2** `summit_extract.py`: Stage A(nano vision = S1 OCR+S2 분해+S3 선별) / Stage B(gpt-5.5 = S4 풀이+S5 분류), structured output(json_schema)
- [x] **T2.3** **정확도 실측**: 역삼 고3 PDF 6쪽 실행 → 위 "Phase 0.5 실측 결과" 참조(분류 수율 낮음=개수형 다수)
- [x] **T2.4** `summit_ingest_api.py`(INGEST_MODE 가드) + ingest API(`upload`/`extract`/`batches`/`batch`/`draft`/`confirm`/`reject`/`page`) + staging 저장. Dockerfile 신규파일 복사 + `/api/config` 추가
- [x] **T2.5** `pages/Ingest.jsx` 3-pane 검토 UI(원본 페이지 이미지 ↔ 추출결과 편집, 확인필요 우선, 정답·분류·형식 확정, AI후보 칩). 사이드바 메뉴는 `/api/config`로 운영 빌드 숨김
- [x] **T2.6** `confirm` → 검증 → `summit_json/{학년}.json` append + 핫리로드. (되돌리기=reject/재편집으로 대체, 별도 일괄취소는 T3로)

### Phase 2.5 / 3 — 확장 (이후)
- [x] **T3.1** 디지털 PDF 텍스트 경로 구현(`stage_a_text`): 페이지 텍스트→문항분해→문법선별→Stage B. 스캔/디지털 통합. (검증: 역삼중 디지털 9쪽 → 어법17/비문법49, 확신분류 8건)
- [ ] **T3.2** 정답 self-consistency(2회 풀이 일치) 신뢰도 보정, 정답지 업로드 옵션
- [ ] **T3.3** 도표/그림 문항 원본 crop 보관 + 플래그
- [ ] **T3.4** 중복 문항 감지(기존 24k 및 써밋 내부)

> **완료 표기 규칙**: 각 작업 끝나면 `[ ]`→`[x]`, "진행 상태"의 *다음 작업*을 갱신, 변경 파일은 커밋하지 않고(사용자가 push 결정) 작업 내용만 보고.

---

## 1. 현재 시스템 분석 (호환성 기준선 · 사실)

코드 직접 분석 결과(붙여넣기 컨텍스트 대신 실제 저장소 기준):

### 1.1 스택
- **백엔드**: Flask `app.py`, Python 3.10, HF Spaces Docker(포트 7860). `python app.py` 실행.
- **프론트**: React 19 + Vite 8 + Tailwind + react-router-dom 7. `frontend/dist` 빌드본을 Flask가 정적 서빙. 라우팅은 `App.jsx`, 메뉴는 `Sidebar.jsx`.
- **데이터(기존)**: `DB/*.json`, `new_json/*.json`. 시작 시 `load_questions_db()`가 전체를 메모리 `QUESTIONS_DB`로 적재(약 24,000문항). `/api/questions`는 **읽기 전용**.
- **LLM**: 프론트에서 사용자 API키(localStorage)로 호출. `llm_api.js`에 **이미지 vision 추출 패턴이 이미 구현**되어 있어 OCR 경로에 재사용 가능.
- **인증/멀티유저**: 없음(단일 운영자 전제).

### 1.2 기존 vs 신규(써밋용) 데이터 — ★중요★

**(기존) 24k 은행** — `new_json/*.json`, `load_questions_db()`가 normalized로 변환해 `/testbank`에서 서빙. 학교명·난이도·연도 등 다수 필드 포함. **이번 작업에서 건드리지 않고 그대로 둔다.**

**(신규) 써밋용 은행** — 별도 디렉토리(`summit_json/`)·별도 로더·별도 라우트(`/summit-bank`). 스키마는 아래처럼 **3축 + 본문만**으로 단순화:

```jsonc
// summit_json/중2.json  →  { "questions": [ ... ] }
{
  "id": "smt-중2-0007",
  "grade": "중2",                 // 축2: 파일 단위 일괄 지정 (중1/중2/중3)
  "grammar_category": "2-7",      // 축1: ★정답 선지가 나타내는 문법★ 소항목 ID 1개
  "question_type": "지문형",      // 축3: 문장형 | 지문형 | 서술형
  "question": "...",              // 문제 발문
  "passage": "...",               // 지문(있으면 지문형 근거). 없으면 ""
  "options": ["① ...", "..."],    // 보기. 서술형이면 [] 가능
  "answer": "4",                  // 정답(선지 번호 또는 서술 답)
  "explanation": "..."            // 참고용 해설(분류 기준 아님, 보관만)
}
```

> 프론트 분류 트리(`grammarCategories.js`)의 **중등(middle) 가지**를 재사용한다. 신규 로더가 `grade`+`grammar_category`로 `categoryId = middle-{n}-grammar-{sub}` 를 만들어 기존 `CategoryTree` 컴포넌트에 그대로 태운다. (고등 확장도 트리에 이미 존재 → 추후 손쉽게 추가 가능)

### 1.3 분류 소항목 정의 (`frontend/src/data/grammarCategories.js`)
- 16개 챕터 + `99-1 미분류`, 소항목 약 76개. 학년별 동일 트리.
- 이 파일이 **소항목 단일 소스** → 분류 프롬프트의 카테고리 목록·검토 UI 드롭다운을 모두 여기서 자동 생성한다. (사람이 챕터를 고쳐도 자동 동기화)

---

## 1.5 실데이터 검증 결과 (★중요★ — `25 역삼 3학년 1학기 중간 기출.pdf`)

실제 업로드 예정 PDF 1건(6쪽)을 페이지 이미지로 렌더링해 검수한 결과:

### 인식 가능성 — 양호
- 150dpi 렌더링에서도 한글/영어/①②/ⓐⓑ/밑줄/보기박스가 식별 가능. 모던 Vision 모델로 OCR+구조화 충분히 가능.
- 단, ⓐ~ⓔ 같은 작은 윗첨자 마커·밑줄 경계 정확도를 위해 **200~300dpi 권장**.

### 발견된 구멍(보완 필수) — 5가지
1. **스캔 PDF (텍스트 레이어 0)**: 6쪽 모두 `get_text()=0`, 페이지당 이미지 60~90개. 즉 **디지털 텍스트 추출 불가 → OCR/Vision이 필수 경로.**
   → MVP 우선순위를 **"스캔/OCR 먼저"** 로 재조정(기존 디지털-텍스트 우선 계획 수정).
2. **정답지 미포함**: 문제지 6쪽에 정답 표기가 전혀 없음. 그런데 분류 기준이 **"정답 선지"** → **정답을 모르면 분류 불가.**
   → 정답 확보 경로 필요(§결정7): (a) 별도 정답지 업로드, (b) AI가 풀어서 추론(저신뢰 플래그), (c) 검토 UI에서 사람이 입력. 권장: **(a) 우선 + 없으면 (b)+(c)**.
3. **문법 문항이 소수**: 33문항 중 대부분이 독해/어휘. 순수 어법은 약 7~9개(Q10, Q17, Q21, Q23, Q24, Q26 등)뿐.
   → 추출 후 **"문법 vs 비문법" 자동 선별 단계** 필요. 비문법(독해·어휘)은 써밋 문법은행 대상 아님 → 스킵/별도보관.
4. **2단 레이아웃 + 페이지 분할 지문**: 한 쪽이 좌·우 2단. `[11~14]`처럼 여러 문항이 한 지문 공유, `[다음 장에 계속]`으로 지문·문항이 페이지를 넘김.
   → 컬럼 분리(crop) 또는 프롬프트로 읽기순서 지시 + 지문-문항 그룹핑 + 인접 페이지 병합 처리.
5. **학년 범위**: 이 파일은 내용상 **고등 수준**(역삼고, diet/Alzheimer 등 지문). "3학년"이 중3/고3 모호.
   → 업로드 학년 선택지에 **고1/고2/고3도 포함**(분류 트리엔 이미 존재. 현 계획의 중1/2/3 한정은 확장).

### 분류 난점(설계 반영)
- **개수형 어법**("어법상 옳은 것의 개수는?")·혼합 어법: 정답 선지가 단일 문법을 안 나타냄 → 핵심 포인트 기준 또는 "확인 필요" 플래그.
- **분류 사각**: 기초 전치사(by/from/with 빈칸 등) 일부는 76소항목에 전용 칸 없음 → `99-1 미분류`.

### Phase 0.5 실측 결과 (2026-06-01, 역삼 고3 PDF · MODEL_OCR=gpt-5.4-nano, MODEL_SOLVE=gpt-5.5)
6쪽 전체 파이프라인 1회 실행(274초). 측정값:
- **추출/선별**: 30문항 인식 → 비문법 16 스킵 → 어법 draft 14건. 문법/비문법 선별은 대체로 정확.
- **분류 수율(중요)**: 14건 중 **확신 분류 2건(12-4, 12-5)**, **미분류(99-1) 12건**. 이유: 이 시험지의 어법 문항이 대부분 **"개수형/모두 고르기"**("옳은 문장의 개수는?", "옳은 것끼리 짝지은 것은?") → 정답이 단일 문법을 나타내지 않음(§1.5 예측한 난점 그대로). → **정답선지 기반 단일분류는 고3 고난도 내신엔 수율이 낮다.** 단일포인트 어법이 많은 중등 시험지에서 재측정 필요.
- **정답 풀이**: 신뢰도 0.98 다수, 일부 "판단 불가"(보기 문장이 Stage A에서 충분히 안 잡힘). **정답지가 없어 정답률을 객관적으로 채점 불가** → 정답지 확보 또는 사람 확정이 필수임을 재확인.
- **OCR 품질(nano)**: 영어 본문/보기는 양호하나 한글 발문에 오타("옳바른","쪼을 때","들어가 지는") 다수 → 검토에서 교정 전제. 필요 시 MODEL_OCR을 mini로 상향 검토.
- **플래깅**: 전 건 "정답 AI 추론—확인 필요" + 저신뢰 플래그 정상 부착(안전망 작동).

→ **결론**: 파이프라인은 기계적으로 정상 작동. 단 (a) 개수형/혼합형 처리 정책, (b) Stage A의 보기문장 캡처 보강, (c) 중등 시험지 재측정, (d) nano OCR 품질 허용범위가 후속 결정 사항.

### 추가 검수 보완사항
- **정확도는 가정하지 말고 측정**: 본격 개발 전, 이 PDF 1건을 파이프라인에 통과시켜 추출/정답/분류 정확도를 사람이 직접 채점하는 **골든셋 평가**(Phase 0.5)를 먼저 한다. 단계별 정확도를 알아야 어디를 보강할지 정해진다.
- **도표·그림 포함 문항**: OCR로 텍스트화하면 도식/표가 손실됨 → 해당 문항은 원본 이미지 crop을 함께 보관 + "이미지 포함" 플래그(검토 시 원본 확인).
- **보기 박스(`<보기>`)·`(A)(B)(C)` 구조**: 박스/괄호 구조를 텍스트로 보존(마커 유지)하도록 OCR 프롬프트에 명시.
- **저작권**: 지문은 외부 저작물일 수 있음(기존 은행도 동일). 내부 활용 전제이며 외부 공개/배포 시 별도 검토 필요.

---

## 2. 핵심 설계 결정

### 결정 1 — 신규 "써밋용" 은행은 기존과 완전 분리
- 저장소: `summit_json/중1.json`, `중2.json`, `중3.json` (학년 = 파일).
- 서빙: 신규 엔드포인트 `GET /api/summit/questions`.
- 화면: 신규 라우트 `/summit-bank`, 사이드바 메뉴 **"기출문제(써밋용)"**.
- 기존 `/testbank`·`/api/questions`·`new_json/`은 **무수정**.

### 결정 2 — 문법 유형은 "정답 선지" 기준 (분류 로직의 핵심 변화)
- **먼저 정답을 확정**하고, 그 **정답이 묻는/나타내는 문법 개념**으로만 소항목을 정한다.
- 어법 오류 찾기형: 정답(=어법상 틀린 선지)이 **위반하는 규칙**이 곧 분류 기준.
- 해설·오답 선지에 다른 문법이 등장해도 **분류에 반영하지 않는다.** (기존 누수 문제 차단)
- 판단 불가 시 `99-1 미분류` + "확인 필요" 플래그.

### 결정 3 — 학년은 파일 단위 일괄
- 업로드 시 `중1/중2/중3` 중 하나 선택 → 그 PDF에서 나온 모든 문항에 동일 적용. AI가 학년을 추론하지 않음(오분류 원천 제거).

### 결정 4 — 형식 판정 규칙 (문장형/지문형/서술형)
우선순위로 결정하고 모호하면 "확인 필요" 플래그:
1. **서술형**: 학생이 직접 쓰는 문항(영작/주관식/빈칸 서술 등, 선택지로 고르는 게 아님).
2. **지문형**: (서술형 아님) 지문(passage)이 있는 문항.
3. **문장형**: (위 둘 아님) 지문 없이 문장/보기만 있는 문항.

### 결정 5 — 저장·배포: 로컬 우선 + git push (확정)
- 추출·검토는 로컬 Flask(`INGEST_MODE=1`, 포트 5001)에서. 확정본을 `summit_json/`에 커밋→push로 운영 반영. HF 휘발성 FS 회피, 운영 서버는 읽기 전용 유지. 검토·쓰기 라우트는 운영 빌드에 미노출.

### 결정 6 — LLM/OCR: 백엔드 호출 (확정)
- 추출·OCR·분류는 서버에서 API 호출(키는 `.env`, `.gitignore` 확인). 스캔본은 OCR/Vision API 사용 허용. 기존 프론트 직접호출은 단어/지문 등 소형 작업에만 유지.

### 결정 7 — 정답 확보: AI 추론 + 사람 확정 (확정)
정답지가 없는 경우가 많으므로(§1.5-2) **AI 추론을 기본 경로**로 한다:
1. **AI 추론(기본)** — 모델이 문항을 직접 풀어 정답 + 근거 + 신뢰도 제시.
2. **검토 UI 확정(필수)** — 검토 화면에 AI 정답을 후보로 띄우고 사람이 최종 확정. **정답 확정 전엔 저장 불가.**
3. **(옵션) 정답지 업로드** — 가진 경우 PDF/이미지/텍스트를 올리면 문항번호로 매칭해 AI추론 대체.
> ★주의: 분류가 정답에 의존하므로 AI가 정답을 틀리면 분류도 틀어진다. **정답 신뢰도를 분류 신뢰도에 연동**, 어법 문항은 기본적으로 "정답 확인 필요"로 강조 표시.

### 결정 8 — 문법/비문법 선별 (실데이터 검증으로 추가)
내신 시험지는 독해·어휘가 다수, 어법은 소수(§1.5-3). 추출 직후 **문법 여부 1차 판정** →
- 문법 → 3축 분류 진행.
- 비문법(독해/어휘/대의 등) → `out_of_scope`로 표시, 써밋 문법은행 미저장(별도 보관함에 모아 추후 활용 여지).

### 결정 9 — ★다단계 프롬프트 + 모델 Tiering★ ("프롬프트 하나"로 하지 않는다)
단일 프롬프트에 OCR·분해·풀이·분류를 다 넣으면 품질·추적성·재시도성이 모두 나빠진다. **단계 분리**가 원칙:

| 단계 | 작업 | 성격 | 모델 | 적용 범위 |
|---|---|---|---|---|
| S1 | OCR(이미지→텍스트, 표기보존) | 지각 | **nano(vision)** | 전 페이지(대량) |
| S2 | 문항 분해 + 지문 그룹핑 | 구조화 | nano | 전 페이지 |
| S3 | 문법/비문법 선별 | 단순분류 | nano | 전 문항 |
| S4 | **정답 풀이(추론)** | **고난도 추론** | **상위 모델(mini/표준)** | **문법 문항만(소수)** |
| S5 | 76 소항목 분류(정답 기반) | 중간 추론 | 상위 모델 | 문법 문항만 |

- **Tiering 이점**: 비싼 추론(S4·S5)은 걸러낸 소수 문법문항에만 → 전체 비용은 거의 nano 수준 유지, 정답/분류 정확도는 확보.
- **nano 단독 주의**: nano로 S4(문제 풀이)까지 처리하면 한국 고교 어법 오답률이 높아 **분류 연쇄 오류 + 사람 교정량 급증.** S4·S5는 상위 모델 권장. (예산상 nano 고정 시 → S4·S5 결과는 전부 "확인 필요"로 강제)
- **정답 이중확인**: S4는 정답+근거+신뢰도를 내고, 가능하면 self-consistency(2회 풀이 일치 여부)로 신뢰도 보정.
- **구조화 출력**: 각 단계는 최신 모델의 structured output(json_schema strict)로 스키마 강제 → 파싱 안정화.
- **재시도/검증 루프**: 단계별 산출물 검증 실패 시 해당 단계만 재호출.

---

## 3. 전체 파이프라인

```
[1] 업로드      시험지 PDF + ★학년(중1~고3) 선택★ + (선택)정답지 PDF/입력
      │
[2] 전처리      스캔PDF→페이지 이미지(200~300dpi). 2단 레이아웃 컬럼 분리.
      │         (디지털PDF면 텍스트레이어 우선)
      │
[3] OCR/문항분해 Vision API로 OCR+문항 경계 인식 → {question, passage, options, answer?, explanation}
      │         + 페이지경계/공유지문 그룹핑. ①②·ⓐⓑ·밑줄(<u>) 표기 재현.
      │
[3.5] 문법선별  문법 여부 판정. 비문법(독해·어휘)은 out_of_scope로 분리(결정8).
      │
[3.7] 정답확보  정답지 매칭 or AI 추론 or 검토단계 입력(결정7).
      │
[4] 3축 분류    · 학년: [1]에서 받은 값 그대로(추론X)
      │         · 형식: 결정4 규칙 (서술형>지문형>문장형)
      │         · 문법유형: ★정답 선지 기반★ 소항목 1개 (LLM, 76목록 제공) + 신뢰도
      │
[5] 플래깅      정답 미확정 / 분류 신뢰도<임계 / 형식·개수형 모호 → "확인 필요"
      │
[6] 검토 대기열  draft 배치로 staging 저장 (문법/비문법 구분)
      │
[7] 웹 검토 UI  /ingest 에서 원문(페이지 이미지) vs 추출결과 나란히 보며 전 필드 수정
      │
[8] 확정        써밋용 스키마 검증 → summit_json/{학년}.json 에 append + 메모리 핫리로드
      │
[9] 배포        git commit & push → HF 재빌드 → 운영 반영
```

---

## 4. 데이터 모델 (staging)

검토 중 데이터는 운영 DB와 분리해 `staging/`에 보관(로컬 작업).

```jsonc
// staging/batch_{ts}.json
{
  "batch_id": "ing-20260531-153012",
  "created_at": "2026-05-31T15:30:12",
  "source_pdf": "중2_기말_2025.pdf",
  "grade": "중2",                      // ★업로드 시 일괄 지정★
  "target_file": "summit_json/중2.json",
  "status": "reviewing",
  "drafts": [
    {
      "draft_id": "ing-...-q07",
      "status": "needs_review",        // needs_review | edited | confirmed | rejected
      "page": 2,
      "raw_source": "원문 텍스트/이미지 crop 참조",
      "extracted": {                   // ← 확정 시 써밋용 스키마로 매핑
        "question": "...", "passage": "...", "options": ["① ...", "..."],
        "answer": "4", "explanation": "...",
        "question_type": "지문형",      // 형식 자동판정
        "grammar_category": "2-7"       // ★정답 선지 기반 분류★
      },
      "confidence": { "extraction": 0.82, "answer": 0.6, "classification": 0.7 },
      "flags": ["정답 신뢰도 낮음", "형식 모호(지문형/서술형)"],
      "ai_candidates": { "grammar_category": ["2-7", "11-2"] }  // 후보(원클릭 선택용)
    }
  ]
}
```

확정 시 매퍼/검증기 강제 규칙:
- `grade` ∈ {중1, 중2, 중3} (배치 값 사용).
- `grammar_category` 는 유효 소항목 ID(`^\d+-\d+$`, `grammarCategories.js` 집합 내).
- `question_type` ∈ {문장형, 지문형, 서술형}.
- `question` 비어있지 않음. `passage` 있으면 형식이 지문형/서술형이어야 일관(불일치 시 플래그).
- 제거 필드는 아예 기록하지 않음(스키마 단순 유지).

---

## 5. 백엔드 API 설계

### 5.1 신규 서빙(운영 포함)
| 메서드·경로 | 역할 |
|---|---|
| `GET /api/summit/questions` | `summit_json/*` 로드본 서빙. 쿼리: `grade`, `grammar`(소항목 또는 챕터 prefix), `type`(문장형/지문형/서술형) |
| `GET /api/summit/categories` | `grammarCategories.js` 동기화 소항목 목록 |

### 5.2 신규 추출/검토(로컬 `INGEST_MODE`에서만 등록)
| 메서드·경로 | 역할 |
|---|---|
| `POST /api/ingest/upload` | PDF + `grade` 수신 → 전처리(텍스트레이어 판별 / 페이지 이미지화) → `job_id` |
| `POST /api/ingest/extract` | job을 LLM(OCR/Vision 포함)으로 문항분해 + 3축 분류 → draft 배치 생성 |
| `GET  /api/ingest/batches` / `GET /api/ingest/batch/<id>` | 검토 대기 목록/상세 |
| `PUT  /api/ingest/draft/<id>` | draft 단건 수정 |
| `POST /api/ingest/confirm` | 확정 → 검증 → `summit_json/{grade}.json` append → 핫리로드 |
| `POST /api/ingest/reject` | draft 폐기 |

설계 메모:
- append는 단일 운영자라 단순 파일락 + atomic write(임시파일→rename).
- 확정 후 써밋용 로더 재호출로 즉시 `/api/summit/questions`에 반영(로컬 미리보기).
- LLM/OCR 호출은 서버(`.env` 키).

---

## 6. 자동 분류 설계 (★정답 선지 기반★)

### 6.1 절차
1. **정답 확정 먼저**: 추출 단계에서 정답(answer)을 식별. 정답 불명확하면 분류 신뢰도↓ + 플래그.
2. **정답이 묻는 문법 개념 도출**: 정답 선지(또는 어법형이면 정답이 위반하는 규칙)에 한정해 어떤 소항목인지 판단.
3. **소항목 매핑(LLM)**: `grammarCategories.js`에서 생성한 76 소항목 + label 목록을 제공하고, **"정답 기준으로만 분류"** 를 명시한 프롬프트로 1개 선택 + 후보 + 신뢰도 출력.
4. **휴리스틱 교차검증(보조)**: 기존 `match_category` 키워드는 **정답/해당 규칙 텍스트에만** 적용해 LLM 결과와 비교(전체 텍스트 스캔 금지 → 누수 차단). 불일치 시 후보 병기 + 플래그.

### 6.2 분류 프롬프트 출력(고정 JSON)
```jsonc
{ "answer": "4",
  "grammar_category": "2-7", "alt": ["11-2"],
  "question_type": "지문형",
  "reason": "정답 선지 ④는 지각동사 see + 목적격보어(원형/현재분사) 용법을 묻는다",
  "confidence": 0.72 }
```
> `grade`는 모델에 묻지 않는다(파일 단위 고정). 카테고리 목록은 코드에서 자동 주입.

### 6.3 플래깅 규칙(초안)
- 정답 미검출 / 분류 신뢰도<0.7 / LLM·휴리스틱 불일치 / 형식 판정 모호(지문+서술 동시 신호) → "확인 필요" 상단 노출.

---

## 7. 프론트엔드 설계

### 7.1 신규 사이드바 메뉴 "기출문제(써밋용)" → `/summit-bank`
- `App.jsx`에 라우트, `Sidebar.jsx`에 메뉴 추가.
- 좌측 필터 패널 3축:
  - **학년**: 중1/중2/중3 탭 또는 체크.
  - **세부 문법 유형**: `CategoryTree`(중등 가지) 재사용, 챕터/소항목 선택.
  - **형식**: 문장형/지문형/서술형 토글.
  - ※ **난이도 필터 없음**(제거).
- 우측: `QuestionCard.jsx` 재사용해 미리보기/인쇄(기존 A4 인쇄 로직 그대로).

### 7.2 검토·수정 UI `/ingest` (로컬 전용, 운영 빌드 숨김)
3-pane: [배치/문항 목록(확인필요 우선)] | [원문 패널(텍스트/페이지 이미지)] | [추출결과 편집].
- 편집 필드: `question / passage / options[] / answer / explanation` + **형식 select(문장형·지문형·서술형)** + **문법 소항목(검색 드롭다운 + AI 후보 칩)**.
- **학년 필드 없음**(배치 값 표시만). **난이도·학교명 필드 없음**.
- 확정 가드: 검증 실패 시 버튼 비활성 + 사유. 확정 후 "최근 확정 취소" 제공.
- 진행률(확정 N/전체 M), 일괄 확정, 폐기.

---

## 8. 전처리(PDF/OCR) 상세
- **디지털 PDF**: 텍스트 레이어 우선 추출(`pdfplumber`/`PyMuPDF`), 페이지 단위.
- **스캔 PDF**: 페이지 이미지화(`PyMuPDF`/`pdf2image`, 200~300dpi) → **OCR/Vision API**로 인식+구조화(별도 OCR엔진 없이 Vision 모델 권장). 기존 `image_url` 패턴 재사용.
- 페이지 경계를 넘는 문항은 인접 페이지 함께 전달. 대용량은 chunk 처리.
- requirements 추가: `PyMuPDF`(또는 `pdf2image`+poppler), `pdfplumber`.

---

## 9. 구현 로드맵

### Phase 0 — 토대 (0.5일)
- 써밋용 스키마 **검증기 + draft→써밋 매퍼**, `/api/summit/categories`(카테고리 소스 공유), 단위테스트.
- `summit_json/{중1,중2,중3}.json` 빈 파일 + 써밋용 로더(`load_summit_db()`) 작성.

### Phase 0.5 — 정확도 실측 (골든셋, 0.5일)  ★개발 전 필수★
- 이 PDF 1건을 단계(S1~S5)별로 한 번 돌려 결과를 사람이 채점: OCR 정확도 / 문항분해 누락 / 문법선별 정/오 / **정답 정답률** / 분류 정답률.
- nano vs 상위모델 비교(특히 S4 정답률)로 모델 Tiering 확정. **"실제로 쓸 만한가"를 숫자로 판단한 뒤 본 개발 진행.**

### Phase 1 — 신규 은행 화면 먼저 (1~2일)
- `GET /api/summit/questions` + `/summit-bank` 페이지 + 사이드바 메뉴.
- (샘플 몇 문항 수동 투입해) 3축 필터·인쇄가 동작하는지 검증. **추출 없이도 은행이 돈다.**

### Phase 2 — 스캔/OCR 추출 + 검토 UI MVP (3~4일)  ※실데이터 검증으로 디지털→스캔 우선 재조정
- `INGEST_MODE` 가드 → `/upload`(학년 선택, 페이지 이미지화 200~300dpi, 컬럼 분리) → `/extract`(Vision OCR+문항분해+문법선별+정답기반 분류) → staging → `/ingest` 3-pane(원문 페이지 이미지) → `/confirm`(append+핫리로드).
- 정답지 업로드/AI추론/수동입력(결정7), 문법/비문법 분리(결정8) 포함.
- **여기까지로 실제 스캔 시험지 반자동 경로 완성.**

### Phase 2.5 — 디지털 PDF 텍스트 경로 (0.5~1일)
- 텍스트 레이어 있는 PDF는 OCR 생략하고 텍스트 추출로 비용·정확도 개선(분기만 추가).

### Phase 4 — 분류 고도화·편의
- 정답기반 분류 정교화, LLM·휴리스틱 교차검증, 되돌리기, 중복 문항 감지.

---

## 10. 리스크 & 주의점
| 리스크 | 대응 |
|---|---|
| 정답 오인식 → 분류 전부 틀어짐 | 정답을 분류 입력으로 쓰므로 **정답 신뢰도 플래깅 강화** + 사람 확정 게이트 |
| 형식 판정 모호(지문+서술) | 우선순위 규칙 + 모호 시 플래그 + 수동 override |
| 소항목 ID 오기입 | 매퍼 정규식·집합 검증(`grammarCategories.js` 기준) |
| 기존 은행과 혼동 | 디렉토리/엔드포인트/라우트 완전 분리, 메뉴 명칭 "기출문제(써밋용)" |
| HF 운영에 쓰기 노출 | `INGEST_MODE` 가드 + 운영 빌드 라우트 미등록 + 메뉴 숨김 |
| API 키 노출 | 서버 `.env`(`.gitignore` 확인) |
| 대용량 PDF 비용/지연 | 페이지 chunk·배치 처리, 진행상태 표시 |

---

## 11. 의사결정 (확정 — 2026-05-31)
1. 저장·배포: ✅ 로컬 우선 + git push
2. LLM/OCR 호출: ✅ 백엔드(서버), OCR API 허용
3. 분류 기준: ✅ **정답 선지 기반 문법유형 / 파일단위 학년(중1·2·3) / 형식(문장형·지문형·서술형)**
4. 부가필드: ✅ 학교명·난이도(상중하) 등 전부 제거
5. 신규 은행: ✅ 사이드바 "기출문제(써밋용)" + `summit_json/` + `/summit-bank`
6. MVP: ✅ Phase 1(은행 화면) → **Phase 2(스캔/OCR 추출, 실데이터가 스캔본이라 우선)** → Phase 2.5(디지털 텍스트)
7. 모델: 미정(추출/분류·OCR용 Vision) — 착수 시 결정.
8. 정답 확보: ✅ **AI 추론이 기본** + 검토 화면에서 사람이 최종 확정(필수). 정답지 업로드는 옵션. 어법 문항은 "정답 확인 필요" 기본 강조.
9. 문법선별: ✅ 추출 직후 문법/비문법 판정, 비문법은 써밋은행 미저장.
10. 학년범위: ✅ 중1~고3 모두 업로드 선택 가능(실데이터가 고등 수준).
11. PDF 종류: ✅ **스캔/디지털 자동 판별**(텍스트 레이어 유무로 분기). 스캔→OCR, 디지털→텍스트추출. 둘 다 지원.
12. 모델 전략: ✅ **단일 프롬프트 아님. 다단계(S1~S5) + Tiering** — nano로 OCR/분해/선별(대량), 정답풀이·분류는 상위 모델(소수 문법문항만). nano 단독 시 S4·S5 결과는 전부 "확인 필요".
13. 사전검증: ✅ Phase 0.5 골든셋으로 단계별 정확도 실측 후 개발(특히 정답률).

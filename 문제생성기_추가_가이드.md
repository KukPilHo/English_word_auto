# 문제 생성기 추가 가이드

새로운 "문제 생성기"(사이드바 메뉴 하나)를 추가할 때 그대로 따라 하는 체크리스트입니다.
가장 최근 추가 사례인 **Reading 일치 짝짓기**(`reading_match`)를 레퍼런스로 삼습니다.

---

## 1. 아키텍처 한눈에

- **프론트엔드**: React + Vite (`frontend/`), 라우팅은 `HashRouter`(URL이 `/#/...` 형태)
- **백엔드**: Flask (`app.py`) — 정적 빌드물(`frontend/dist`)과 일부 API(`/api/...`)만 서빙
- **AI 호출은 100% 브라우저에서** 일어남. 사용자의 API 키로 OpenAI/Anthropic을 직접 호출(`lib/llm_api.js`). 서버는 AI 생성에 관여하지 않음
- **상태**: React Context (`store/`) — 페이지별 상태 슬라이스 + 히스토리(IndexedDB) + 관리자 모드
- **결과물**: 화면 카드 렌더 + Word(.docx) 내보내기(`lib/docx_export.js`)

### 데이터 흐름
```
페이지(.jsx)  →  question_logic.js(생성)  →  화면 카드 렌더(.jsx)
                       │                          │
                  llm_api.js               docx_export.js (.docx)
                  (AI 호출)                        │
                       │                    useAutoSave → HistoryContext (자동 저장)
                  결과 = questions[]
```

각 문항(`question`) 객체는 아래 공통 필드를 가진다:
```js
{
  number: 1,            // 화면/문서용 번호
  typeId: 'reading_match', // ★ 분기 키 (docx_export, 복원 등에서 사용)
  type: 'ReadingMatch',    // 대분류(레거시 호환용 라벨)
  instruction: '...',   // 발문
  // 이하 유형별 자유 필드: passage / statements / choices / options / answer 등
}
```

---

## 2. 새 생성기 추가 체크리스트 (파일 8곳 + 빌드)

> 아래 순서대로 건드리면 누락이 없다. 슬러그 예시: `reading_match`(typeId) / `readingMatch`(세션 타입) / `/reading-match`(라우트).

| # | 파일 | 할 일 |
|---|------|-------|
| 1 | `frontend/src/lib/question_logic.js` | 생성 함수 추가 (AI 또는 로컬 로직). `typeId`/`type` 부여한 배열 반환 |
| 2 | `frontend/src/lib/docx_export.js` | `renderXxx()` 작성 + `switch(q.typeId)`에 `case` 추가 |
| 3 | `frontend/src/pages/Xxx.jsx` | **신규** 페이지 UI (입력 → 생성 → 결과 카드 → 다운로드) |
| 4 | `frontend/src/App.jsx` | `import` + `<Route path="..." element={<Xxx/>} />` |
| 5 | `frontend/src/components/Sidebar.jsx` | `navItems`에 메뉴 1줄 추가(필요시 `adminOnly: true`) |
| 6 | `frontend/src/store/AppContext.jsx` | 상태 슬라이스 `xxxState` 추가 + provider value에 노출 |
| 7 | `frontend/src/store/HistoryContext.jsx` | setter 구조분해 + `loadSession`에 복원 `case` 추가 |
| 8 | `frontend/src/lib/sessionSchemas.js` | `SESSION_TYPES` / `TYPE_LABELS` / `TYPE_ROUTES` / `TYPE_COLORS` / `TYPE_ICONS` / 제목·요약 `switch` |
| ✓ | — | `cd frontend && npx vite build` 로 컴파일 확인 |

`hooks/useAutoSave.js`는 타입에 무관하게 동작하므로 **수정 불필요**(주석의 타입 목록만 원하면 갱신).

---

## 3. 단계별 상세

### 3-1. 생성 로직 — `lib/question_logic.js`

AI 호출 계약:
```js
import { generateContent } from './llm_api';
// generateContent(apiKey, model, systemPrompt, userPrompt)
//   → response_format: json_object 강제, 파싱된 JS 객체를 그대로 반환
//   → model에 'claude' 포함 시 Anthropic, 아니면 OpenAI 엔드포인트 자동 선택
const res = await generateContent(apiKey, model, systemPrompt, userPrompt);
const questions = (res.questions || []).map(...);
```

원칙(중요):
- **정답·조합처럼 틀리면 안 되는 부분은 AI에 맡기지 말고 JS에서 결정적으로 생성**하라.
  - 예) `reading_match`은 AI에게 "7개 진술 + 정확히 2개 참(is_correct)"만 시키고,
    선택지 짝 5개와 정답은 `buildMatchChoices()`가 코드로 만든다(셔플 포함).
  - 같은 패턴: `generateType1Questions`(단어매칭)도 보기/오답/정답을 전부 코드로 생성.
- 셔플/랜덤추출 유틸은 파일 상단 `shuffle()`, `pickRandom()` 재사용.
- 프롬프트에는 **개수 강제, 원문 보존 여부, 오답 함정 유형, 난이도 반영**을 명시.
- 반환 시 각 문항에 `typeId`/`type`을 반드시 부여.

레퍼런스: `generateReadingMatchQuestions` / `buildMatchChoices`, `generateReadingOXQuestions`.

### 3-2. Word 내보내기 — `lib/docx_export.js`
- `renderXxx(q)` 가 `docx` Paragraph/Table 배열을 반환.
- 공통 헬퍼 재사용: `createInstructionHeader(num, instruction)`, `createBoxedContent(paras)`(테두리 박스).
- 메인 `exportToDocx`의 `switch (q.typeId)`에 `case 'xxx': docElements.push(...renderXxx(q)); break;` 추가.

### 3-3. 페이지 — `pages/Xxx.jsx`
`ReadingMatch.jsx`를 복사해 시작하는 게 가장 빠르다. 핵심 골격:
```jsx
const { xxxState, setXxxState } = useAppState();
const { apiKey, model } = useSettings();
const { saveToHistory } = useAutoSave('xxx');     // 세션 타입 슬러그
// 생성:
const generated = await generateXxxQuestions(apiKey, model, passageText, count, difficulty);
const numbered = generated.map((q, i) => ({ ...q, number: i + 1 }));
updateState({ questions: numbered });
await saveToHistory({ passageText }, { difficulty, questionCount }, { questions: numbered }, { questionCount: numbered.length });
// 다운로드:
await exportToDocx(questions, '영어문제_xxx.docx');
```
- 난이도 옵션은 `DIFFICULTY_LEVELS`(`lib/questionTypes.js`) 사용, 기본값은 보통 인덱스 `[3]`(고1).
- 입력 검증(지문 30자 이상, API 키 등)과 진행 상태 표시 패턴은 기존 페이지 그대로.

### 3-4. 라우팅 — `App.jsx`
```jsx
import Xxx from './pages/Xxx';
// <Routes> 안:
<Route path="/xxx" element={<Xxx />} />
```

### 3-5. 사이드바 — `components/Sidebar.jsx`
```js
// navItems 배열에 추가 (icon은 lucide-react에서 import)
{ name: '메뉴 이름', path: '/xxx', icon: SomeIcon, desc: '한 줄 설명' },
// 관리자에게만 보이게 하려면:  adminOnly: true  한 줄만 더
```
`adminOnly: true`가 있으면 관리자 모드(§5)일 때만 노출된다.

### 3-6. 상태 슬라이스 — `store/AppContext.jsx`
```js
const [xxxState, setXxxState] = useState({
  passageText: DEFAULT_READING_PASSAGE,
  questions: [],
  difficulty: DIFFICULTY_LEVELS[3].value,
  questionCount: 1,
  generationProgress: null,
});
// ...Provider value 객체에 추가:
xxxState, setXxxState,
```

### 3-7. 히스토리 복원 — `store/HistoryContext.jsx`
```js
const { ..., setXxxState } = useAppState();          // 상단 구조분해에 추가
// loadSession 의 switch(type) 에 추가:
case 'xxx':
  setXxxState(prev => ({
    ...prev,
    passageText: input.passageText || prev.passageText,
    difficulty: settings.difficulty || prev.difficulty,
    questionCount: settings.questionCount || prev.questionCount,
    questions: result.questions || [],
    generationProgress: null,
  }));
  break;
```

### 3-8. 세션 스키마 — `lib/sessionSchemas.js`
6곳 모두 한 줄씩 추가:
```js
SESSION_TYPES.XXX = 'xxx'
TYPE_LABELS.xxx  = '짧은라벨'      // 히스토리 배지 텍스트
TYPE_ROUTES.xxx  = '/xxx'
TYPE_COLORS.xxx  = { bg, text, border }  // tailwind 클래스
TYPE_ICONS.xxx   = '🔗'
// generateSessionTitle() / getSessionSummary() 의 switch 에 case 추가
//   (개수 기반이면 READING_OX case 에 함께 묶으면 됨)
```

---

## 4. 난이도 상수

`lib/questionTypes.js`의 `DIFFICULTY_LEVELS` 사용. value에 Lexile 목표가 들어있어 프롬프트에 그대로 끼워넣으면 됨. (중1~고3, 인덱스 0~5)

---

## 5. 관리자 모드 (메뉴 숨기기)

- 진입: URL에 `?admin=<코드>` → 서버(`app.py`의 `/api/admin/verify`)가 환경변수 `ADMIN_CODE`와 대조 → 통과 시 `localStorage`에 저장(이후 코드 없이도 유지). 종료는 사이드바 하단 버튼.
- 판정 중앙화: `store/AdminContext.jsx` 의 `useAdmin().isAdmin`. **추후 구글 계정 화이트리스트로 바꿀 때 이 파일만 교체**하면 됨.
- 특정 생성기를 관리자 전용으로: `Sidebar.jsx` 의 해당 `navItems` 항목에 `adminOnly: true` 한 줄.
- 운영(HF)에서 관리자 모드를 쓰려면 Space **Settings → Secrets** 에 `ADMIN_CODE` 설정(로컬은 `.env`).

---

## 6. 빌드 & 배포 (Hugging Face)

- HF Space는 **`main` 브랜치**에서만 자동 빌드·배포한다. 다른 브랜치(`staging` 등)에 푸시하면 배포되지 않음(스테이징 용도).
- `frontend/dist`는 git에 커밋하지 않는다. **Dockerfile이 빌드 때 `npm run build`로 직접 생성**하므로, `main`에만 올라가면 새 페이지가 자동 포함된다.
- 반영 절차:
  ```
  git checkout main && git merge staging
  git push hf main            # → HF가 Docker 자동 리빌드(2~5분)
  ```
- 배포 후 안 보이면: ① HF 빌드 완료(Running) 대기 → ② 브라우저 **하드 리프레시**(Cmd/Ctrl+Shift+R) → ③ Space Logs 탭에서 빌드 에러 확인.
- 운영 이미지 주의(`Dockerfile`): `summit_pdf.py`/`summit_extract.py`/`summit_ingest_api.py`/`.env`는 **운영에 미포함**(추출·검토는 로컬 `INGEST_MODE` 전용).

---

## 7. 기존 생성기 레퍼런스

| 메뉴 | 라우트 | typeId | 생성 방식 | 비고 |
|------|--------|--------|-----------|------|
| 빈칸 매칭(문장생성) | `/` | `word_matching` / `blank_matching` / `single_blank` | 1=로컬, 2·3=AI | 단어 기반(TypeB) |
| 영영풀이(지문기반) | `/passage` | (TypeA) | AI | 지문+단어 |
| Reading 일치/불일치 | `/reading-ox` | `reading_ox` | AI | 5개 진술 O/X 조합 |
| **Reading 일치 짝짓기** | `/reading-match` | `reading_match` | AI(진술)+JS(조합) | **신규 레퍼런스** |
| 다풀백 지문 변형 | `/variation` | (variation) | AI | 원문 유지·지문만 변형 |

새 유형 추가 시 가장 가까운 레퍼런스를 복제해 시작하면 빠르다. 단순 O/X·짝짓기류는 `reading-match`, 단어/보기 매칭류는 `word_matching`을 참고.
